machine:
  services:
    - docker

dependencies:
  override:
    - echo "override section"

test:
  override:
    - echo "no tests yet =("

deployment:
  hub:
    branch: master
    commands:
      # - docker build -t robzhu/nodecolor .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      # - docker push robzhu/nodecolor
      # - install the rancher-upgrade-prep tool
      - git clone https://github.com/robzhu/rancher-upgrade-prep
      - cp -a ./rancher-upgrade-prep/. ./
      - npm install
      - sudo chmod +x ./deploy_to_rancher.sh
      - sudo ./deploy_to_rancher.sh